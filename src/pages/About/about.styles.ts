import { motion } from "framer-motion";
import styled from "styled-components";

const StyledAbout = styled(motion.section).attrs({
    className: "relative py-12 px-8 text-white bg-myPrimary-100",
})``;

export default StyledAbout;
